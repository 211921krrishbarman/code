#include <stdio.h>
#include<math.h>

int main() {
    int markA, markB;
    printf("Enter your percentage in subjects A and B");
    
    scanf("%u %u", &markA ,&markB);
    
    if (markA>=55 && markB>=45) printf("Qualified");
    
    else if (markA>=55 && markB>=55 || markB>=45) printf("Qualified");
    
    else if (markA>=65 && markB<=45)
    printf("Reappear in B")
    ;
    else printf("Failed!");
    return 0;
}
